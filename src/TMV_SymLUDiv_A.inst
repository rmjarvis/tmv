
#define CT std::complex<T>

#define DefLUD(RT,T)\
template void SymLU_Decompose(const SymMatrixView<T >& A, \
    const VectorView<T >& xD, size_t* P, T& det); \
template void SymInvert_2x2<true,T >(T& a, T& b, T& c, T* dd); \
template void SymInvert_2x2<false,T >(T& a, T& b, T& c, T* dd); \

DefLUD(T,T)
#ifdef INST_COMPLEX
DefLUD(T,CT)
#endif

#undef DefLUD

#undef CT
