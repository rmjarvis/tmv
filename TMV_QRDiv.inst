#define CT complex<T>

#define DefFuncs(T) \
  template QRDiv<T >::QRDiv(const GenMatrix<T >& A, bool _inplace); \
  template bool QRDiv<T >::CheckDecomp(const BaseMatrix<T >& m, \
      ostream* fout) const; \
  template T QRDiv<T >::Det() const; \
  template Matrix<T > QRDiv<T >::GetQ() const; \

DefFuncs(T)
DefFuncs(CT)

#undef DefFuncs

#undef CT

