#define CT complex<T>

#define DefFuncs(T) \
  template QRPDiv<T >::QRPDiv(const GenMatrix<T >& A, bool _inplace); \
  template bool QRPDiv<T >::CheckDecomp(const BaseMatrix<T >& m, \
      ostream* fout) const; \
  template T QRPDiv<T >::Det() const; \
  template Matrix<T > QRPDiv<T >::GetQ() const; \

DefFuncs(T)
DefFuncs(CT)

#undef DefFuncs

#undef CT
