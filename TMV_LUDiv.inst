
#define CT complex<T>

#define DefLUD(T)\
  template class LUDiv<T >; \
  template void LU_Decompose(const MatrixView<T >& lu, size_t* p, T& det); \

DefLUD(T);
DefLUD(CT);
#undef DefLUD

#define DefDivEq(T,T1)\
  template void LU_LDivEq(const GenMatrix<T1 >& A,\
      const size_t* P, const MatrixView<T >& b); \
  template void LU_RDivEq(const GenMatrix<T1 >& A,\
      const size_t* P, const MatrixView<T >& b); \

DefDivEq(T,T);
DefDivEq(CT,T);
DefDivEq(CT,CT);
#undef DefDivEq

#undef CT
