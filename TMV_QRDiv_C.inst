#define CT complex<T>

#define DefInv(T) \
template void QRDiv<T >::Inverse(const MatrixView<T >& minv) const; \
template void QRDiv<T >::DoInverseATA(const MatrixView<T >& minv) const; \

DefInv(T)
DefInv(CT)

#undef CT

