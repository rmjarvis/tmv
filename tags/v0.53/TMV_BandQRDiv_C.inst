#define CT complex<T>

#define DefInv(T) \
template void BandQRDiv<T >::Inverse(const MatrixView<T >& minv) const; \
template void BandQRDiv<T >::DoInverseATA(const MatrixView<T >& minv) const; \

DefInv(T)
DefInv(CT)

#undef CT

