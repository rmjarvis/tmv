#define CT complex<T>

#define DefInv(T) \
template void QRPDiv<T >::Inverse(const MatrixView<T >& minv) const; \
template void QRPDiv<T >::DoInverseATA(const MatrixView<T >& minv) const; \

DefInv(T)
DefInv(CT)

#undef CT

