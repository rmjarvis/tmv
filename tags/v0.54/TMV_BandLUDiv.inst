
#define CT complex<T>

#define DefFuncs(T) \
  template BandLUDiv<T >::BandLUDiv(const GenBandMatrix<T >& A, bool _inplace); \
  template bool BandLUDiv<T >::CheckDecomp( \
      const BaseMatrix<T >& m, ostream* fout) const; \
  template T BandLUDiv<T >::Det() const; \
  template LowerTriMatrix<T,UnitDiag> BandLUDiv<T >::GetL() const; \
  template const size_t* BandLUDiv<T > ::GetP() const; \

DefFuncs(T)
DefFuncs(CT)

#undef DefFuncs

#undef CT
