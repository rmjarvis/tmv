
#define CT complex<T>

#define DefFuncs(T) \
  template bool UpperTriDiv<T >::CheckDecomp( \
      const BaseMatrix<T >& m, ostream* fout) const; \
  template bool LowerTriDiv<T >::CheckDecomp( \
      const BaseMatrix<T >& m, ostream* fout) const; \
  template T UpperTriDiv<T >::Det() const; \
  template T LowerTriDiv<T >::Det() const; \

DefFuncs(T)
DefFuncs(CT)

#undef DefFuncs

#undef CT
