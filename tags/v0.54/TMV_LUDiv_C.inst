#define CT complex<T>

#define DefInv(T) \
template void LUDiv<T >::Inverse(const MatrixView<T >& minv) const; \
template void LUDiv<T >::InverseATA(const MatrixView<T >& minv) const; \

DefInv(T)
DefInv(CT)

#undef CT

