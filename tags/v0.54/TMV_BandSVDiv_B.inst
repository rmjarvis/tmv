
#define CT complex<T>

#define DefInv(T) \
template void BandSVDiv<T >::Inverse(const MatrixView<T >& minv) const; \
template void BandSVDiv<T >::DoInverseATA(const MatrixView<T >& minv) const; \

DefInv(T)
DefInv(CT)

#undef CT

