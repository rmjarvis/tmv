
#define CT std::complex<T>

#define DefLUD(RT,T)\
template void LDL_Decompose(const SymMatrixView<T >& A, \
    const VectorView<T >& xD, int* P, RT& ld, T& sd); \
template void LDL_Decompose(const SymMatrixView<T >& A, \
    const SymBandMatrixView<T >& D, int* P); \

DefLUD(T,T)
#ifdef INST_COMPLEX
DefLUD(T,CT)
#endif

#undef DefLUD

#undef CT
