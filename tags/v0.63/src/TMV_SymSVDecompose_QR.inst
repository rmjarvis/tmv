
#define CT std::complex<T>

#define DefSVD(RT,T)\
template void EigenFromTridiagonal_QR(MVP<T > U, \
    const VectorView<RT >& D, const VectorView<RT >& E); \
template void HermTridiagonalChopSmallElements( \
    const VectorView<T >& D, const VectorView<T >& E); \

DefSVD(T,T)
#ifdef INST_COMPLEX
DefSVD(T,CT)
#endif

#undef DefSVD

#undef CT

