
#define CT std::complex<T>

#define DefLUD(T)\
template void LU_Decompose(const MatrixView<T >& lu, int* p, int& signdet); \
template void LU_Decompose(const MatrixView<T >& lu, int* p); \

DefLUD(T)
#ifdef INST_COMPLEX
DefLUD(CT)
#endif

#undef DefLUD

#undef CT
