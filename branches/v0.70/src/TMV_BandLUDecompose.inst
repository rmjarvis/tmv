
#define CT std::complex<T>

#define DefLUD(T) \
template void LU_Decompose(BandMatrixView<T > lu, int* p, int Anhi); \
template void LU_Decompose( \
    const GenBandMatrix<T >& A, LowerTriMatrixView<T > L, \
    BandMatrixView<T > U, int* P); \

DefLUD(T)
#ifdef INST_COMPLEX
DefLUD(CT)
#endif

#undef DefLUD

#undef CT

