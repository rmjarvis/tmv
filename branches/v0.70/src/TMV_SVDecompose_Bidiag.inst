
#define CT std::complex<T>

#define DefSVD(RT,T)\
template void Bidiagonalize( \
    const MatrixView<T >& A, const VectorView<T >& Ubeta, \
    const VectorView<T >& Vbeta, const VectorView<RT >& D, \
    const VectorView<RT >& E, T& det); \

DefSVD(T,T)
#ifdef INST_COMPLEX
DefSVD(T,CT)
#endif

#undef DefSVD

#undef CT

