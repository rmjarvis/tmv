
#define CT std::complex<T>

#define DefSVD(RT,T)\
template void DoSVDecomposeFromBidiagonal( \
    MVP<T > U, VectorView<RT> D, VectorView<RT> E, MVP<T > V, \
    bool UisI, bool VisI); \
template void SV_Decompose(MatrixView<T > U, \
    DiagMatrixView<RT > S, MVP<T > V, RT& ld, T& det, bool StoreU); \
template void SV_Decompose(MatrixView<T > U, \
    DiagMatrixView<RT > S, MatrixView<T > V, bool StoreU); \
template void SV_Decompose(MatrixView<T > U, \
    DiagMatrixView<RT > S, bool StoreU); \
template void SV_DecomposeFromBidiagonal(MVP<T > U, \
    VectorView<RT > D, VectorView<RT > E, MVP<T > V, bool SetUV);  \

DefSVD(T,T)
#ifdef INST_COMPLEX
DefSVD(T,CT)
#endif

#undef DefSVD

#undef CT

